repositories:
  - name: bitnami
    url: https://charts.bitnami.com/bitnami
releases:
- name: my-redmine
  namespace: default
  chart: bitnami/redmine
  version: 33.0.3
  values:
    - ./values.yaml
    - ./redminepass.yaml.gotmpl
